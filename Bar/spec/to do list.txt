- BarDetail : 	+ get bar GPS at first view if does not GPS of bar in db
		+ mark complete bar with complete image
		+ notify to capture ,upload to complete bar.
		+ image local store : with directory : bar/bar_id/*.png.
		+ capture image : only save image with direct location.detect capture button to get location.if user can 		not allow GPS, show alert.
		+ add info button at right of gallery to help user how can they complete bar. --> if complete, change it to 		complete image.
		+ using attractive background image.
- City : -add more info : complete image if city has all completed bars.

- App in charse.

- request city : form with user feedback and send mail to server

- add splash screen and app icon.

- background for all screen.